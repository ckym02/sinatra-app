<div>
  <p><%= @subject %></p>
  <p><%= @content %></p>
</div>
<div class="flex-box gap-2 col-6 mx-auto">
  <button id="button-tag" class="btn btn-secondary btn-lg" onclick='clickButton("<%= "http://localhost:4567/memos/#{@memo_id}/edit" %>")'>変更</button>
  <form action="<%= "/memos/#{@memo_id}" %>" method="post">
    <input type="hidden" name="_method" value="delete" />
    <input type="submit" class="btn btn-light btn-lg" value="削除" onClick="return confirm('本当に削除しますか？')">
  </form>
</div>
