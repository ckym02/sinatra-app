<button onclick="location.href='/memos/new'">追加する</button>
<ul>
  <% @memos.each do |memo| %>
    <li>
      <a href="<%= "/memos/#{memo[0]}" %>" ><%= memo[1]['subject'] %></a>
    </li>
  <% end %>
</ul>
